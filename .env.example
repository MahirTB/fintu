# App Configuration
# Default credentials for local development if not set are admin / admin.
# Change these values to secure your site.
APP_USERNAME=admin
APP_PASSWORD=admin

# Database Settings
# Set this in production. Local development will fallback to SQLite (finance.db) automatically.
# DATABASE_URL=postgresql://user:password@localhost:5432/fintu
